apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: astronomy-data
  namespace: astronomy
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 10Gi
