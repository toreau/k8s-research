apiVersion: skiperator.kartverket.no/v1alpha1
kind: Application
metadata:
  name: hello
  namespace: sample
spec:
  image: k8s-testapp:latest
  port: 8080
  resources:
    requests:
      cpu: 25m
      memory: 128Mi
  replicas:
    min: 2
    max: 4
    targetCpuUtilization: 50
